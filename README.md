
  # React App with Bootstrap

  This is a code bundle for React App with Bootstrap. The original project is available at https://www.figma.com/design/zEZGtbvJC9niwTLncttgV8/React-App-with-Bootstrap.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  